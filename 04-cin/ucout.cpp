#include <iostream>
using namespace std;

int main()
{
    float result = 1.0f;
    int a = 2, b = 3;
    cout << "Initial result: " << result << endl; // "endl" is used to insert a newline and flush the stream
    cout << "Values: a = " << a << ", b = " << b << endl;
    return 0;
}